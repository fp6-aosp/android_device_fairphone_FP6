<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- When true use the linux /dev/input/event subsystem to detect the switch changes
    on the headphone/microphone jack. When false use the older uevent framework -->
    <bool name="config_useDevInputEventForAudioJack">true</bool>

    <!-- Needed for Rounded corner (RC) enablement. -->
    <dimen name="rounded_corner_radius">20dp</dimen>
    <dimen name="rounded_corner_content_padding">0dp</dimen>

    <!-- A string config in svg path format for the main display shape.
         The below shape takes into account rounded corners of radius 20dp. -->
    <string name="config_mainDisplayShape" translatable="false">M20 0h1040s20 0 20 20v2360s0 20 -20 20h-1040s-20 0 -20 -20v-2360s0 -20 20 -20</string>

    <!-- Default list of files pinned by the Pinner Service -->
    <string-array translatable="false" name="config_defaultPinnerServiceFiles">
        <item>"/system/framework/arm64/boot-QPerformance.oat"</item>
        <item>"/system/framework/arm64/boot-UxPerformance.oat"</item>
        <item>"/system/framework/framework.jar"</item>
        <item>"/system/framework/services.jar"</item>
        <item>"/apex/com.android.media/javalib/updatable-media.jar"</item>
        <item>"/vendor/lib64/libllvm-qgl.so"</item>
        <item>"/vendor/lib64/libllvm-glnext.so"</item>
    </string-array>

    <!-- List of biometric sensors on the device, in decreasing strength. Consumed by AuthService
         when registering authenticators with BiometricService. Format must be ID:Modality:Strength,
         where: IDs are unique per device, Modality as defined in BiometricAuthenticator.java,
         and Strength as defined in Authenticators.java -->
    <array name="config_biometric_sensors" translatable="false" />

    <!-- Control whether the always on display mode is available. This should only be enabled on
         devices where the display has been tuned to be power efficient in DOZE and/or DOZE_SUSPEND
         states. -->
    <bool name="config_dozeAlwaysOnDisplayAvailable">true</bool>

    <!-- Control whether the always on display mode is enabled by default. This value will be used
         during initialization when the setting is still null. -->
    <bool name="config_dozeAlwaysOnEnabled">false</bool>

    <!-- Boolean indicating whether the wifi chipset has dual frequency band support for SAP -->
    <bool translatable="false" name="config_wifi_dual_sap_mode_enabled">false</bool>
</resources>
